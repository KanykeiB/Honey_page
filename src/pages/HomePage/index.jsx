import React from 'react';
import ProductList from '../../containers/product-list';

const HomePage = () => {
    return (
        <div>
            <ProductList/>
        </div>
    );
};

export default HomePage;